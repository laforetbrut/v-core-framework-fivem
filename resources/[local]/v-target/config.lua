-- v-target | shared config
Config = {}

Config.Debug = false

-- Hold this key to open the interaction eye (default LEFT ALT).
Config.Key = 'LMENU'

-- Max reach of the eye (metres) and how far a zone/entity option is still offered.
Config.MaxDistance = 7.0

-- Select an option with these number keys (1..N) or with left mouse button (= option 1).
-- Numbers shown in the NUI match these.
